---
# Production inventory
# Add your production hosts here

all:
  children:
    workstations:
      hosts:
        sofia-laptop:
          ansible_host: 192.168.42.210
          ansible_user: sofia
          target_user: sofia
        # Example workstation configuration:
        # my-workstation:
        #   ansible_host: 192.168.1.100
        #   ansible_user: adam
        #   target_user: adam
        sk-pf3d0a9t:
          ansible_host: 192.168.42.205
          ansible_user: adam
          target_user: adam
          ansible_python_interpreter: /usr/bin/python3
        legion9:
          ansible_host: 192.168.42.206
          ansible_user: adam
          target_user: adam
          ansible_python_interpreter: /usr/bin/python3

    servers:
      hosts:
        # Example server configuration:
        # my-server:
        #   ansible_host: 192.168.1.200
        #   ansible_user: root

    lxd_hosts:
      hosts:
        # Hosts that run LXD containers:
        # lxd-host:
        #   ansible_host: 192.168.1.50
        #   ansible_user: adam

    lxd_containers:
      hosts:
        # LXD containers (dynamically populated):
        # container-1:
        #   ansible_host: 10.0.0.10
        #   ansible_user: ubuntu

    # -------------------------------------------------------------------------
    # Gotify deployment groups
    # -------------------------------------------------------------------------

    gotify_hosts:
      hosts:
        linode-gotify:
          ansible_host: 139.162.226.242
          ansible_user: adam
          ansible_python_interpreter: /usr/bin/python3

    gotify_container:
      hosts:
        # Populated at runtime by playbooks/gotify.yml via ansible.builtin.add_host.
        # The container is created on linode-gotify (139.162.226.242) by Play 1.
        # Connection: community.general.lxd (lxc exec on the host)

    # -------------------------------------------------------------------------
    # Conduwuit (Matrix homeserver) deployment groups
    # -------------------------------------------------------------------------

    conduwuit_hosts:
      hosts:
        potwor:
          ansible_host: 192.168.10.5
          ansible_user: adam
          ansible_python_interpreter: /usr/bin/python3

    conduwuit_container:
      hosts:
        # Populated at runtime by playbooks/conduwuit.yml via ansible.builtin.add_host.
        # The container is created on potwor (192.168.10.5) by Play 1.
        # Connection: community.general.lxd (lxc exec on the host)
