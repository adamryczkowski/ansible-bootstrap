---
# Inventory for Mikolaj's Legion7
# Target: Set up Roo-Code with MCP servers

all:
  hosts:
    mikolaj-legion7:
      ansible_host: 192.168.42.203
      ansible_user: mikolaj
      ansible_ssh_private_key_file: ~/.ssh/id_ed25519
      ansible_python_interpreter: /usr/bin/python3

  vars:
    # Target user for configuration
    target_user: mikolaj

    # Disable AI prompts (different user, no access to adam's repo)
    roo_code_ai_prompts:
      enabled: true
